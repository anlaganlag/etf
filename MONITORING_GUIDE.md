# Real-time Monitoring Guide (实盘监控指南)

本指南用于指导交易员（或自动化脚本）如何监控 **Robust Volatility State Machine** 的运行状态。

---

## 1. 核心监控指标 (The Big Three)

实盘中，你不需要盯着每一笔交易，只需要盯住这三个核心指标：

### A. Broken Ratio (BR) - 地震波强度
*   **定义**: 当前市场中，$Z < -2.5$ (K_CRASH) 的标的占比。
*   **来源**: 日志文件 `output_log_final.txt` 或实盘控制台输出。
*   **关注线**:
    *   🟢 **< 10%**: 极度安全，岁月静好。
    *   🟡 **10% ~ 20%**: 暴风雨前的气压下降，密切关注。
    *   🟠 **> 20%**: **警报拉响**，系统应自动切入 `CAUTION` (半仓)。
    *   🔴 **> 40%**: **最高警报**，系统应空仓。

### B. Risk Scaler - 系统姿态
*   **定义**: 当前策略实际允许的仓位上限 (1.0 / 0.5 / 0.0)。
*   **正常状态**: 绝大多数时间应为 `1.0`。
*   **异常状态**: 如果市场看似平静，但 Scaler 变为 0.5，说明**底层结构正在恶化**（很多股票在阴跌破位），这是系统比人敏感的地方，**相信系统**。

### C. Z-Score (Sample) - 个股体温
*   **抽查**: 随机抽查 3-5 只持仓股的 Z-Score。
*   **标准**: 它们应该都在 `-1.6` 以上。如果有持仓股 Z < -2.0 且没被卖出，说明代码逻辑有 Bug。

---

## 2. 日志关键词速查 (Log Watch)

在实盘运行日志中，使用 `grep` 或 `Ctrl+F` 搜索以下关键词：

| Keyword | Meaning | Action Needed |
| :--- | :--- | :--- |
| `METAGATE_LOG` | 每日状态心跳包 | 用于绘制 BR 曲线 |
| `🚦 METAGATE` | **状态切换事件** | **重点关注！** 意味着系统改变了防御姿态。 |
| `Sync [SELL]` | 调仓卖出动作 | 确认委托已成交 |
| `Value Warning` | 资金/持仓核对异常 | 检查券商资金是否因为 T+1 没到账 |

---

## 3. 每日巡检 SOP (Daily Routine)

1.  **收盘前 (14:45)**:
    *   检查预估的 `Broken Ratio` (如果有实时计算)。
    *   确认 `Risk Scaler` 是否符合预期。
2.  **收盘后 (15:10)**:
    *   检查日志 `METAGATE_LOG` 最后一行。
    *   记录当天的 BR 值，画在 Excel/Dashboard 上观察趋势。
    *   **关键**: 如果 BR 连续 3 天上升（例如 5% -> 12% -> 18%），做好明天可能半仓的心理准备。

---

## 4. 紧急干预手册 (Emergency Manual)

尽管我们移除了自动的 Capital Gate，但作为操作者，你拥有最终裁决权。

**Condition Red (熔断条件)**:
*   实盘净值回撤 **> 30%** (从最高点)。
*   或者：实盘遭遇连续 3 次 **Risk Scaler 误判**（比如空仓踏空暴涨）。

**Action**:
1.  手动停止脚本。
2.  清空所有仓位。
3.  重新评估参数 (K_ENTRY / K_CRASH)。
4.  **不要在盘中修代码！**

---

**监控的核心不是干预，而是确认“系统还在预期的轨道上”。**
